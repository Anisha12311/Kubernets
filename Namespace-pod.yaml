apiVersion: v1
kind : Pod
metadata: 
   name : myapp-pod
   labels : 
      app : team1
      type : front-end
spec : 
    containers:
    - name : nginx-containers
      image : nginx 

    nodeSelector:
      size : Large